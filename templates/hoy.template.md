---
title: "TODAY — quick capture"
note: "Tick each set, write weight × reps. When done, tell your assistant 'done' and it archives it."
generado: "gym.py next + gym.py readiness + gym.py compute (targets verbatim)"
---

<!-- FIXED SKELETON — same sections, same order, every session. Fill the {slots}; never restyle. -->

# 🔥 {TYPE} · {focus muscles}

**Why {TYPE}:** {1–2 lines from gym.py next — deficit + recovery + what's excluded and why}

**Bodyweight:** ____ {unit}

> {🟢/🟡/🔴} **READINESS {pct}%** ({date} · trend {a→b→c}) → {directive from gym.py readiness, VERBATIM}.
<!-- If the score arrives after the sheet was generated, update ONLY this line, keeping this exact format. -->

## A · {Exercise name} — {series}×{reps} · {peso}
{`meta` from gym.py compute — VERBATIM, do not rewrite}
- ☐ ____ × ____
- ☐ ____ × ____
- ☐ ____ × ____

## B · {Exercise name} — {series}×{reps} · {peso}
{`meta` from gym.py compute — VERBATIM, do not rewrite}
- ☐ ____ × ____
- ☐ ____ × ____
- ☐ ____ × ____

<!-- ...one heading block per exercise, one checkbox per set -->

> **Goal #1 today:** {the session's overload, from targets — e.g. "complete ALL sets" / "add load where compute says so"}.
> **Notes:**
